---
status: Draft
owner: "Asher"
reviewers: ["Tech Lead", "Security Lead"]
updated_at: "2026-09-03"
feature_size: "M"
---

# Review — brainstorm（复审）

**Gate result: `PASS`**

> 上一轮 `review-2026-09-02.md` 返回 `CHANGES REQUESTED`（10 条 finding）。本轮为复审：核实 2 条 de-scope + 5 条 fix-now 是否落地正确，并复核整条 §5 AC 链无回归。

## Scope

- 变更面：上轮 10 条 finding 的处置结果（2 条 de-scope 的文档/代码改动 + 5 条 fix-now 的代码/测试改动）。
- 测试：`python -m pytest -q` → **62 passed**（上轮 58 → +4 新测试）；`ruff`、`mypy` 均干净。
- 评审方式：独立 clean-context `reviewer` 子代理复审。

## 复审结论

**Stage-1（阻断）F1–F4 全部闭合：**

| # | 处置 | 核验 |
|---|---|---|
| F1 | de-scope：删 `cross_session_write/read`，改「构造隔离」 | 契约/代码/测试/spec/sad 全对齐，无 raise/引用残留 |
| F2 | de-scope：手动停止降为进程内，CLI stop best-effort | spec §3 / cli.md / test-plan 三方一致 |
| F3 | fix：CLI `llm_factory` 注入 + `ConvergingLLM` e2e | 真实经 CLI 驱动 moderator 收敛（非 flag 检查） |
| F4 | fix：`EchoRole` 集成测试 | 自定义 role 真实跑过 `run_session`、发言落桌 |

**Stage-2 fix-now（F8/F9/F10）核实有效：**

- F8 `stop.py` 直接结束路径补发 `session.stopped`（载荷与 loop 一致，无双重发射）。
- F9 `test_moderator.py` 全循环后读 `read_events` 断言 `invalid_choice` 落库。
- F10 `data-model.md` 改为 `:events:stream`，与 `namespaces.py` 一致。

**Stage-2 defer（F5/F6/F7）已记录，非静默丢弃：** `roadmap.md`（步骤 2 引擎泛化吸收）+ `spec.md §8` OQ（owner: Tech Lead）+ `spec.md §3` non-goal 兜底。三者均 `AC: n/a`，无 AC 被 uncover。

## 本轮发现并已修复（4 条 stage-2 文档残留）

复审仅发现 4 条 **de-scope 未清理干净的文档同步项**（无 AC 影响），已全部修复：

1. `contracts/api-sync-report.md` — 残留 2 条 cross-session 行 +「6 个 AC 派生」计数 + F4 映射行 → 删两码、改 4 码、改「构造隔离」。
2. `test-plan.md` — 契约覆盖描述仍写「AC-02/03/06/06b/13 error 码」→ 改「AC-02/03/13/15（AC-06/06b 无 error 码）」。
3. `sad.md §8` — 仍写「截断 N 可配置」与 deferred F6 冲突 → 改「v1 固定 N=20，可配置化随引擎泛化」。
4. `tasks/t2-domain.md` — 历史任务记录残留旧错误码/旧落点 → 加「De-scope 注」标注以当前契约为准。

## 裁决

- 无未决 stage-1 finding；所有 finding 已 fix / de-scope / defer。
- `PASS` —— 可进入 `ship`。
- 遗留 defer（F5/F6/F7）归入 roadmap 步骤 2（引擎泛化），不阻塞本特性 ship。
